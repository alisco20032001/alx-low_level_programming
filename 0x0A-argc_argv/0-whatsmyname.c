#include <stdio.h>

/**
 *_main - a program that prints the number of
 * arguments passed into it.
 * @argc: number of argument
 * @argv: array name
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
