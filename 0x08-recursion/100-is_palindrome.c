#include "main.h"

/**
 * _palindrome.c - function that returns 1 if a string
 * is a palindrome and 0 if not.
 * @s: string
 *
 * Return: 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int is_palindrome(char *s);
{
	if (*s == 0 || *(s + 1) == 0)
		return 1;
	
	_putchar = ('\n');
	ispalindrome(str + 1);
}

