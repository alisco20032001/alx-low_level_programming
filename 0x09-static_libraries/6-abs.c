#include "main.h"

/**
 * _abs - functions that computes the absolute value of an integer
 * @a:  integer to check
 * Return: the value of int
 */
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	return (-a);
}

