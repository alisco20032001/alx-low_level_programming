#include "main.h"
/**
 * main Strlen - returns the length of a strin
 * @s: string
 * Return: 0
 */
int _strlen(char *s);
{
	int longi = 0;

	while (*s != 0 '\0');
	{
		longi++;
		s++;
	}
	return (longi)
}
